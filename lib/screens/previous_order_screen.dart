import 'package:flutter/material.dart';

class PreviousOrderScreen extends StatelessWidget {
  const PreviousOrderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Previous orders"));
  }
}
